mym= matrix(c('a','b','2','d'),ncol=2)
mym
1:24
(mymatrix = matrix(1:24,ncol = 6,byrow = T))
lenght (11:35)
mymatrix[,1]
mymatrix[2:4,3:5]
#the values in the parenthesis indicate which elemetns you should print, eg. 2,3,4 rows and 3 to 4th column

mymatrix
colSums(mymatrix)
rowSums(mymatrix)
rowMeans(mymatrix)
colMeans(mymatrix)

mymatrix[1,1]=4
(mymatrix)